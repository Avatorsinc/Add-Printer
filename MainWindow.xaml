<Window x:Class="Add_Printer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Add_Printer"
        Title="Add Printer"
        Height="600" Width="800"
        Style="{StaticResource MaterialDesignWindow}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Grid.Row="0" Margin="16">
            <TextBlock Text="Add Printer"
                 FontSize="24"
                 FontWeight="Bold"
                 Foreground="{DynamicResource MaterialDesignBody}"
                 VerticalAlignment="Center"
                 DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal"
                  DockPanel.Dock="Right">
                <materialDesign:PackIcon Kind="InformationOutline"
                                 Width="32" Height="32"
                                 Foreground="{DynamicResource MaterialDesignBody}"
                                 ToolTip="About"
                                 MouseLeftButtonUp="Info_Click"
                                 Margin="0,0,8,0"/>
                <materialDesign:PackIcon Kind="ThemeLightDark"
                                 Width="32" Height="32"
                                 Foreground="{DynamicResource MaterialDesignBody}"
                                 ToolTip="Toggle Light/Dark"
                                 MouseLeftButtonUp="SwitchTheme_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- MAIN CONTENT -->
        <StackPanel Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="20"
                Width="600">
            <TextBox x:Name="SearchTextBox"
               materialDesign:HintAssist.Hint="Enter printer name"
               Style="{DynamicResource MaterialDesignOutlinedTextBox}"
               Margin="0,0,0,16"/>

            <Button Content="Search Printers"
              Width="150" Height="40"
              Click="SearchPrinters_Click"
              Style="{DynamicResource MaterialDesignRaisedButton}"
              Margin="0,0,0,20"/>

            <ListView x:Name="PrintersListView"
                Height="200"
                MouseDoubleClick="PrintersListView_MouseDoubleClick"
                Style="{DynamicResource MaterialDesignListView}">
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Connect Printer" Click="ConnectPrinterMenu_Click"/>
                        <MenuItem Header="Install Drivers…" Click="InstallDrivers_Click"/>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Printer Name"
                            Width="500"
                            DisplayMemberBinding="{Binding DisplayName}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <!-- FOOTER -->
        <DockPanel Grid.Row="2" Margin="16">
            <TextBlock Text="Developed by Workplace Management"
                 FontSize="12"
                 Foreground="{DynamicResource MaterialDesignBodySecondary}"
                 VerticalAlignment="Center"
                 DockPanel.Dock="Left"/>
            <Image Source="pack://application:,,,/Assets/logo.png"
             Width="48" Height="48"
             VerticalAlignment="Center"
             DockPanel.Dock="Right"/>
        </DockPanel>
    </Grid>
</Window>
